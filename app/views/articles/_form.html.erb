<%= form_for(article) do |f| %>
  <% if article.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(article.errors.count, "error") %> prohibited this from being from being saved:</h2>

      <ul>
      <% article.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

<div class="form-group">
  <h3>Share This.</h3>
</div>

  <div class="form-group">
    <%= f.label :url %>
    <%= f.text_field :url, class: 'form-control' %>
  </div>
  <div class="instructions">
    <p>
      Please make sure to include http:// at the beginning of each link.
    </p>
  </div>

  <div class="actions">
    <%= f.submit class: 'btn btn-success btn-sm' %>
  </div>
<% end %>
